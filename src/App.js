import React, { Component } from 'react';
import Routes from './routes/Routes.js';

class App extends Component {
  render() {
    return (
      <Routes /> //Returns current route page and displays it to the app.
    );
  }
}

export default App;
